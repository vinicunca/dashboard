export enum THEME_TYPE {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum SETTING_BUTTON_POSITION {
  AUTO = 'auto',
  HEADER = 'header',
  FIXED = 'fixed',
}

export enum PERMISSION_MODE {
  ROLE = 'ROLE',
  ADMIN = 'ADMIN',
  ROUTE_MAPPING = 'ROUTE_MAPPING',
}

export enum SESSION_TIMEOUT_PROCESSING {
  ROUTE_JUMP,
  PAGE_COVERAGE,
}

export enum CONTENT_TYPE {
  // auto width
  FULL = 'full',
  // fixed width
  FIXED = 'fixed',
}

// Route switching animation
export enum ROUTER_TRANSITION {
  ZOOM_FADE = 'zoom-fade',
  ZOOM_OUT = 'zoom-out',
  FADE_SIDE = 'fade-slide',
  FADE = 'fade',
  FADE_BOTTOM = 'fade-bottom',
  FADE_SCALE = 'fade-scale',
}

